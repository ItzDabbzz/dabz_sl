Creator feature code lives here.

- Dashboard/database route files stay in `app/` as wrappers.
- Creator-facing UI and workflow code moves into `features/creator/*`.
- Shared authenticated server fetch helpers live under `server/`.
