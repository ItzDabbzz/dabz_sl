Auth feature code lives here.

- Route implementations live under `features/auth/pages/*`.
- Reusable auth UI lives under `features/auth/components/*`.
- `app/(auth)/*` remains as thin route wrappers only.
