Implementation for marketplace routes lives here.

- `app/dashboard/tools/marketplace/*` keeps route entry files only.
- `app/api/tools/marketplace/*` keeps route handler entry files only.
- UI, server logic, and shared types move into this feature folder so the Next.js routing tree stays thin.
